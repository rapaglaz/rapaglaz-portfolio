name: Release Quality Check
description: 'Minimal quality gate for release - only i18n validation. Format and lint already verified in PR checks.'

runs:
  using: 'composite'
  steps:
    - name: Setup environment
      uses: ./.github/actions/workspace-setup-action

    - name: Run i18n validation
      shell: bash
      run: pnpm run i18n:check
