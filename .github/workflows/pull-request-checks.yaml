name: Pull Request Checks

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    outputs:
      app: ${{ steps.filter.outputs.app }}
      e2e: ${{ steps.filter.outputs.e2e }}
      i18n: ${{ steps.filter.outputs.i18n }}
      ci: ${{ steps.filter.outputs.ci }}
      deps: ${{ steps.filter.outputs.deps }}
      run_quality: ${{ steps.aggregate.outputs.run_quality }}
      run_e2e: ${{ steps.aggregate.outputs.run_e2e }}
      run_build: ${{ steps.aggregate.outputs.run_build }}
      run_workflow_lint: ${{ steps.aggregate.outputs.run_workflow_lint }}
      only_deps: ${{ steps.aggregate.outputs.only_deps }}
      only_ignored: ${{ steps.aggregate.outputs.only_ignored }}
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Detect changed file paths
        uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3.0.2
        id: filter
        with:
          filters: |
            app:
              - 'src/**'
              - 'public/**'
              - 'angular.json'
              - 'tsconfig*.json'
              - 'eslint.config.mjs'
            e2e:
              - 'e2e/**/*.ts'
              - 'playwright.config.*'
            i18n:
              - 'public/i18n/**'
            ci:
              - '.github/workflows/**'
            deps:
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'pnpm-workspace.yaml'
              - '.npmrc'

      - name: Aggregate change flags
        id: aggregate
        shell: bash
        run: |
          run_quality=false
          run_e2e=false
          run_build=false
          run_workflow_lint=false
          only_deps=false

          if [[ "${{ steps.filter.outputs.ci }}" == "true" ]]; then
            run_workflow_lint=true
          fi

          if [[ "${{ steps.filter.outputs.app }}" == "true" || "${{ steps.filter.outputs.i18n }}" == "true" || "${{ steps.filter.outputs.deps }}" == "true" ]]; then
            run_quality=true
          fi

          if [[ "${{ steps.filter.outputs.app }}" == "true" || "${{ steps.filter.outputs.e2e }}" == "true" ]]; then
            run_e2e=true
          fi

          if [[ "${{ steps.filter.outputs.app }}" == "true" || "${{ steps.filter.outputs.i18n }}" == "true" || "${{ steps.filter.outputs.e2e }}" == "true" ]]; then
            run_build=true
          fi

          if [[ "${{ steps.filter.outputs.deps }}" == "true" && "${{ steps.filter.outputs.app }}" != "true" && "${{ steps.filter.outputs.i18n }}" != "true" && "${{ steps.filter.outputs.e2e }}" != "true" && "${{ steps.filter.outputs.ci }}" != "true" ]]; then
            only_deps=true
          fi

          if [[ "${{ steps.filter.outputs.app }}" != "true" && "${{ steps.filter.outputs.i18n }}" != "true" && "${{ steps.filter.outputs.e2e }}" != "true" && "${{ steps.filter.outputs.ci }}" != "true" && "${{ steps.filter.outputs.deps }}" != "true" ]]; then
            only_ignored=true
          fi

          {
            echo "run_quality=$run_quality"
            echo "run_e2e=$run_e2e"
            echo "run_build=$run_build"
            echo "run_workflow_lint=$run_workflow_lint"
            echo "only_deps=$only_deps"
            echo "only_ignored=$only_ignored"
          } >> "$GITHUB_OUTPUT"

  quality-check:
    runs-on: ubuntu-latest
    needs: [detect-changes]
    if: ${{ needs.detect-changes.outputs.run_quality == 'true' && needs.detect-changes.outputs.only_deps != 'true' }}
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Run Quality Check Action
        uses: ./.github/actions/pr-check-action
        env:
          RUN_TESTS: ${{ needs.detect-changes.outputs.app == 'true' }}
          RUN_I18N_CHECK: ${{ needs.detect-changes.outputs.app == 'true' || needs.detect-changes.outputs.i18n == 'true' }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_BRANCH: ${{ github.head_ref }}
          PR_BASE: ${{ github.base_ref }}

  workflow-lint:
    runs-on: ubuntu-latest
    needs: [detect-changes]
    if: ${{ needs.detect-changes.outputs.run_workflow_lint == 'true' || needs.detect-changes.outputs.only_ignored == 'true' }}
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Run actionlint
        run: |
          bash <(curl https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash)
          ./actionlint -color
        shell: bash

  e2e-tests:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.58.0-noble@sha256:35c7d48b4ccaf3aca5018f5f1bf7f50c7da7d61d176c530741f4f2e9ca336c34
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.run_e2e == 'true' && needs.detect-changes.outputs.only_deps != 'true' }}
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Build application
        uses: ./.github/actions/build-action

      - name: Run Playwright tests
        run: pnpm exec playwright test

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: playwright-report-pr
          path: playwright-report/
          retention-days: 7

  build:
    runs-on: ubuntu-latest
    needs: [detect-changes]
    if: ${{ needs.detect-changes.outputs.run_build == 'true' && needs.detect-changes.outputs.only_deps != 'true' }}
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Build application
        uses: ./.github/actions/build-action
        with:
          base-href: /${{ github.event.repository.name }}/previews/pr-${{ github.event.pull_request.number }}/

      - name: Upload build artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: build-artifact-pr
          path: dist/rapaglaz-portfolio/browser
          retention-days: 1

  dependency-check:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.deps == 'true' && needs.detect-changes.outputs.only_deps == 'true' }}
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Setup environment
        uses: ./.github/actions/workspace-setup-action

  deploy-preview:
    runs-on: ubuntu-latest
    needs: build
    if: (github.event_name == 'pull_request' || github.event_name == 'pull_request_target') && needs.build.result == 'success'
    permissions:
      contents: write
      pages: write
      deployments: write
      pull-requests: write
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Download Build Artifact
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: build-artifact-pr
          path: build-output

      - name: Deploy to GH Pages
        uses: JamesIves/github-pages-deploy-action@d92aa235d04922e8f08b40ce78cc5442fcfbfa2f # v4
        with:
          folder: build-output
          target-folder: previews/pr-${{ github.event.pull_request.number }}
          branch: gh-pages
          clean: false
          commit-message: 'docs: deploy preview for PR #${{ github.event.pull_request.number }}'
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update PR Comment
        uses: marocchino/sticky-pull-request-comment@773744901bac0e8cbb5a0dc842800d45e9b2b405 # v2
        with:
          number: ${{ github.event.pull_request.number }}
          header: preview
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          message: |
            ### Preview

            [https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/previews/pr-${{ github.event.pull_request.number }}/](https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/previews/pr-${{ github.event.pull_request.number }}/)

            ---
            *Last updated: ${{ github.event.pull_request.updated_at }}*
