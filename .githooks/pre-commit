#!/bin/sh
set -e

REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

if ! command -v pnpm >/dev/null 2>&1; then
  echo "pnpm is required to run format + lint pre-commit hook." >&2
  exit 1
fi

pnpm format:write
pnpm lint
